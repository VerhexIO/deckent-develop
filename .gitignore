node_modules/
dist/
coverage/
*.tgz

# Deckent runtime state
.tasks/
.locks/
.dashboard

# Brain runtime (Memory V2 — exports/ are tracked, raw runtime files are not)
.brain/MEMORY.md
.brain/RETRO.md
.brain/DEBT.md
.brain/PATTERNS.md
.brain/ERRORS.md
.brain/PROJECT-IDENTITY.md
.brain/directives-backup/

# Memory V2 SQLite DB (binary, rebuilt from exports)
.brain/memory.db
.brain/memory.db-wal
.brain/memory.db-shm
.brain/memory.db.bak-*
.brain/memory.db.backup-*
.brain/sprints/

# Connector media artifacts (bot-received photos/files — personal data, never committed)
.deckent/artifacts/

# Sprint runtime artifacts (Memory V2 DB-first — content in memory.db)
.brain/archive/sprint-*-tasks/
.brain/archive/sprint-*.md
.brain/archive/retro-sprint-*.md
.brain/archive/pre-v2/
.brain/archive/sprint-*_*.pid
.brain/archive/*.snapshot.json
.brain/archive/*.sprint-state.json

# Sprint metrics (runtime + deckent archive)
.deckent/archive/metrics/
.deckent/sprint-*-metrics.jsonl
# Sprint tool-inventory (runtime artifact — 427-011/429-011)
.deckent/sprint-*-tool-inventory.txt
.deckent/runtime/tool-inventory/

# Test artifacts
.test/
.test-e2e-sprint-*/
.test-e2e-sprint-*
.playwright-mcp/

# Deckent runtime config & state
.deckent/agents/temp-*/
.deckent/routing/
.deckent/config.json
.deckent/config.json.bak.*
# DESK-1 daemon handshake file — carries live tokens, never committed (born-496)
.deckent/serve-daemon.json
# DESK-1 desktop sub-package build/runtime outputs (born-496)
src/desktop/node_modules
src/desktop/out/
src/desktop/release/
src/desktop/dist-app/
src/desktop/*.tsbuildinfo
# Identity provider runtime DB (ADR-092) — local state, like .brain/memory.db
.deckent/identity.db
.deckent/identity.db-wal
.deckent/identity.db-shm
.deckent/crashes/*.log
!.deckent/crashes/.gitkeep
.deckent/settings.local.json
.deckent/ci-baseline.json
.deckent/safety-point.json
.deckent/project-stack.json
.deckent/sprint-state.json
.deckent/plugins/*
!.deckent/plugins/.gitkeep
.deckent/runtime/jobs/
.deckent/usage/
.deckent/cache/
.deckent/traces/
# do-flow runtime state + serve tokens (dogfood-449) — tokens never committed
.deckent/runtime/api-token
.deckent/runtime/terminal-token
.deckent/runtime/run-flow-store/
.deckent/runtime/prompt-lint.jsonl
.deckent/runtime/run-status-read-model.json
.deckent/runtime/archive/
.deckent/pause-state.json
# eye-loop isolated daemon workspace inside desktop sub-package (590)
src/desktop/.deckent/
.deckent/training/
.deckent/sprint-*-baseline.json
.deckent/metrics.jsonl

# Managed docs runtime config (private — template in src/cli/commands/init-templates/)
.deckent/settings/docs.json

# Nervous System runtime (ADR-040 — logs, history, pending, IPC dirs, all regenerated)
.deckent/nervous/

# Per-sprint ephemeral artifacts (events, metrics, gate, pre-archive — regenerated each run)
.deckent/recently-works/

# Sprint ephemeral artifacts (IPC, events, checkpoints — regenerated each run)
.deckent/sprint-*-ipc/
.deckent/sprint-*-events.jsonl
.deckent/sprint-*-seq
.deckent/sprint-*-checkpoint-seq
.deckent/sprint-*-checkpoint.json
.deckent/sprint-*-gate.json
.deckent/sprint-*-pre-archive.*
.deckent/sprint-*-panic-*.json
.deckent/sprint-*-*-result.json
.deckent/sprint-*-subagent-prompts/
.deckent/sprint-*-merge-runbook.sh
.deckent/sprint-*-worktree-setup.sh
.deckent/sprint-*-dispatch-locks.json
.deckent/runtime/evaluations/
.deckent/pids/

# Autonomous + general runtime state (GITIGN-RT, 2026-06-17 — local-only, never tracked)
.deckent/*.pid
.deckent/notify-log.jsonl
.deckent/autonomous-events.jsonl
.deckent/autonomous-seq
.deckent/panic-ipc/
.deckent/sprint-*-start.log
.deckent/evaluations/
.deckent/autonomous/backlog.json
.deckent/autonomous/pending.json
.deckent/autonomous/*-task.md

.claude/scheduled_tasks.lock
.claude/settings.local.json

# Deckent runtime artifacts (per-sprint, ephemeral)
# Cache-prefix stability: these files change frequently and alter git-status snapshot,
# breaking system-prompt cache coherence. Gitignored to preserve prompt-cache prefix
# identity across sprint runs (analysis: docs/analysis/2026-06-10-weekly-limit-reverse-engineering.md §8)
.deckent/heartbeat.pid
.deckent/sprint.lock
.deckent/mcp-server.pid
.deckent/*-gate.json

# Environment
.env
.env.*
.deck

# Credentials & secrets
*.pem
*.key
credentials.json

# Audit HMAC chain key (Sprint 179 W5-12, I4 invariant — machine-local, mode 0600)
.deckent/audit-key

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Dashboard sub-project
src/dashboard/node_modules
src/dashboard/dist
src/dashboard/tsconfig.tsbuildinfo

# SQLite URI test artifacts (regression guard, Sprint 175)
file:audit-idem-*

# Local diagnostic outputs (test logs, npm cache, personal notes)
.npm-cache/
vitest-out.txt
kendimenot.md
.tmp/

# npm cache (gitignored — regen on install)
.tmp-npm/
.tmp-vitest-result.json

# Sprint runtime debris (gitignored — regen on each sprint)
.vitest-out*.txt
.test-e2e*

# Core memory backup (local mirror of user-level memory — never publish)
docs/core-memory/

# Internal planning/strategy docs are TRACKED here (this is the private DEVELOP repo,
# VerhexIO/deckent-develop, ADR-065). They were LOST once (2026-06-08) when gitignored +
# swept by git clean/stash — gitignoring removed git's protection. Track them, don't ignore.
# The separate PUBLIC product repo excludes these via its own export tooling, not gitignore here.
.brain/archive/
!.brain/archive/
# Test temp dir leaks + editor backups (not source)
.test-archive-debt-*/
*.md.bak

# Runtime cache (rewrites timestamp every run — not source)
.deckent/provider-cache.json
vit-*.log
.deckent/archive/sprints/
docs/audits/sprint-*/load-test-report.md
docs/audits/sprint-139/dead-code-report.md

# Worker container HOME-leak guard + secrets (Sprint 214 — never commit)
.bash_history
.deckent/.keyring
.codex/config.toml
.codex/tmp/
.gemini/projects.json
.npm/
.tmp-test/
.test-e2e-*/
.brain/archive/

# Stray non-project doc (Claude system-prompt artifact, not deckent source) — keep on disk, out of repo
claude-fable-x.md
.deckent/prompts/
.deckent/approvals/
.brain/heartbeat-log.md

# W7 (2026-07-07): kok tmp/test-artiklari repoya giremez
.tmp/
.tmp-npm/
.test-archive-debt-*/
.deckent/stats/

# Alperen (2026-07-12): bot ses-deneyi — lokal kalir, repoya girmez
examples/voice-wrapper/

# Alperen (2026-07-28): analiz-notlari lokal kalir, repoya girmez
.analysis/

# Alperen (2026-08-27): accepted post-product vision portfolio local kalir, repoya girmez
docs/post-product/

# Runtime debris (2026-07-28 temizlik — makine-lokal, asla commit edilmez)
.deckent-execution-lock-authority.anchor.json
.deckent/autonomous/autonomous.db-wal
.deckent/autonomous/autonomous.db-shm
.deckent/mcp-writer.lease
.deckent/runtime/invocations.db
.deckent/runtime/invocations.db-shm
.deckent/runtime/invocations.db-wal
.deckent/runtime/acceptance-reconciliation.db
.deckent/runtime/acceptance-reconciliation.db-shm
.deckent/runtime/acceptance-reconciliation.db-wal
.deckent/runtime/resource-log-dogfood.jsonl
.deckent/runtime/scheduler-shadow/
.deckent/recovery-snapshots/
.deckent/archive/
# OpenRouter model-listesi TTL cache (provider-cache.json emsali)
.deckent/settings/openrouter-models.json

# ─── Analiz / arşiv snapshot'ları (2026-08-02, Alperen) ───────────────────
# Bu üç ağaç kalıcı repo yükü değil, dönemsel kanıt snapshot'ıdır.
# DİKKAT: hepsi hâlihazırda TRACKED — bu girdiler yalnız YENİ dosyaları
# tutar. Mevcutları çıkarmak ayrı ve bilinçli bir `git rm --cached` adımıdır
# (git geçmişi her hâlükârda korur: docs/archive → commit 97b91e69f).
.analysis/
docs/archive/
docs/analysis/

# W3: in-repo native addon build output (compiled per-host, never committed)
native/exec-authority/build/
native/exec-authority/prebuilds/
native/exec-authority/.build-stage-*/

# SQLite WAL side-files (runtime churn, never committed)
.deckent/*.db-shm
.deckent/*.db-wal

# Repo cleanup 2026-09-04 (owner-approved G3): runtime data and historical archive are never tracked
# Owner-approved 2026-09-06: keep local runtime evidence on disk, outside Git.
/.deckent/settings/repl-history
/.deckent/runtime/owner-notifications.jsonl
/.deckent/runtime/owner-notification-receipts.jsonl
.deckent/settings/resource-log.jsonl
.deckent/provider-execution-observations.db
.deckent/provider-execution-observations.db-wal
.deckent/provider-execution-observations.db-shm
.deckent/models.db
.deckent/models.db-wal
.deckent/models.db-shm
.deckent/capability-audit.jsonl
.deckent/autonomous/autonomous.db
.deckent/autonomous/autonomous.db-wal
.deckent/autonomous/autonomous.db-shm
.deckent/runtime/logs/
.deckent/runtime/evaluations/
.deckent/runtime/closure-staging/
docs/archive/
